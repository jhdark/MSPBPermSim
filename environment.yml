name: MSPB-env

channels:
  - conda-forge
  - defaults

dependencies:
  - pip
  - fenics
  - matplotlib
  - numpy<1.25  # needed to avoid deprecation warning
  - ipykernel  # needed to run jupyter notebooks
  - scipy
  - pip:
    - festim>1.1